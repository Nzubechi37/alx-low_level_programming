#include <stdio.h>
#include "main.h"
/**
 * _isupper - this functon checks for upper casem
 * @c : the charactet to b checkedt
 *
 * return last digit
 */

int _isupper(int c)


if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}
